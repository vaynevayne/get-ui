
## Hello MDX
 
import { useState } from 'react'
import {Table} from '@get-ui/react'

 
export function Counter({ children }) {
 
  console.log('table',Table);
  
  const [count, setCount] = useState(0)
  return (
    <>

    <button onClick={() => setCount(count + 1)} className="text-3xl font-bold underline">
      {children}
      {count}
    </button>
    table
   
    </>
  )
}
 
<Counter>**Clicks**: </Counter>